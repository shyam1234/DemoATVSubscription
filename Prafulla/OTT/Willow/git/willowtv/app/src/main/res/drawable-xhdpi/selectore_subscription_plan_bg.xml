<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:drawable="@color/transparent" android:state_window_focused="false" />

    <!-- Even though these two point to the same resource, have two states so the drawable will invalidate itself when coming out of pressed state. -->
    <item android:drawable="@drawable/subscription_plan_bg" android:state_focused="false" />
    <item android:drawable="@drawable/subscription_plan_bg_white" android:state_focused="true" />

</selector>